<Window x:Class="Labb2Database.Shops"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Labb2Database"
        mc:Ignorable="d"
        Title="Shops" Height="450" Width="800">
    <Grid>
        <ListView x:Name="shopsListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding ID}" Width="50"/>
                    <GridViewColumn Header="StoreName" DisplayMemberBinding="{Binding StoreName}" Width="150"/>
                    <GridViewColumn Header="AddressLine" DisplayMemberBinding="{Binding AddressLine}" Width="150"/>
                    <GridViewColumn Header="City" DisplayMemberBinding="{Binding City}" Width="100"/>
                    <GridViewColumn Header="State" DisplayMemberBinding="{Binding State}" Width="100"/>
                    <GridViewColumn Header="PostalCode" DisplayMemberBinding="{Binding PostalCode}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>